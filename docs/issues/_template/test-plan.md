# テスト計画

`README.md` の受入条件と整合させる。`/verify-issue` がこのチェックリストを順に消化する。

## 自動テスト

| 種別 | 対象 | 期待挙動 | ファイル |
| --- | --- | --- | --- |
| Unit (api) | | | `apps/api/...` |
| Unit (web) | | | `apps/web/...` |
| E2E | | | |

## 手動検証

- [ ] **<シナリオ 1>**: 操作手順 → 期待結果
- [ ] **<シナリオ 2>**: 操作手順 → 期待結果

## 非機能・回帰

- [ ] 既存機能が壊れていない (smoke)
- [ ] エラーパスが妥当（401 / 403 / 404 / 500 など）
- [ ] パフォーマンス影響なし（必要なら計測）

## 検証エビデンス

スクショ・録画は `assets/` に置き、ファイル名で対応シナリオがわかるようにする。

| シナリオ | エビデンス |
| --- | --- |
| | |
